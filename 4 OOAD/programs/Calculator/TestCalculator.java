package Calculator;

public class TestCalculator {
    public boolean testAdd(int res, int cmp) {
        return res == cmp;
    }
    public boolean testSub(int res, int cmp) {
        return res == cmp;
    }
    public boolean testMul(int res, int cmp) {
        return res == cmp;
    }
    public boolean testDiv(int res, int cmp) {
        return res == cmp;
    }
}
